function getName() : string {
    var question = "What's your name?"
    while true {
        print(question)
        var name = readline()
        
        print("Are you sure this is your name? y/n")

        var result = readline()
        if (result == "y")
            return name
        else
            question = "Then what's your name?"
    }
}